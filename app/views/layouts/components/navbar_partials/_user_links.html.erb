<div class="nav-item dropdown">
      <% if user_signed_in? %>
        <% if current_user.photo.attached? %>
          <img class="avatar dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" src="<%= cl_image_path current_user.photo.key %>" />
        <% else %>
          <i class="fas fa-user-ninja" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></i>
          <!-- <img class="avatar dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" src="" /> -->
        <% end %>
    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
      <%= link_to edit_user_registration_path do %>
        <button class="dropdown-item" type="button">User panel</button>
      <% end %>
      <a class="dropdown-item" href="#">Another action</a>
      <div class="dropdown-divider"></div>
      <%= link_to destroy_user_session_path do %>
        <button class="dropdown-item" type="button">Log out</button>
      <% end %>
    </div>
    <% end %>
</div>
