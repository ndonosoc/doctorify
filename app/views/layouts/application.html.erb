<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Doctorify</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_pack_tag 'application', media: 'all' %>
  </head>
  <body>

    <div class="min-vh-100 d-flex flex-column"> <!-- This div fixes the footer as it shifts the row vision to column -->
      <%= render 'layouts/components/navbar' %>
      <% padding = params[:controller] == "pages" && params[:action] == "home" ? '' : 'pt-5' %>
      <div class="mflex-grow-1 <%= padding %>" >
        <%= render 'shared/flashes' %>
          <%= yield %>
      </div>

      <div class="footer flex-grow-0 bg-white mt-auto">
        <div class="footer-links">
          <a href="#"><i class="fab fa-instagram"></i></a>
          <a href="#"><i class="fab fa-facebook"></i></a>
          <a href="#"><i class="fab fa-twitter"></i></a>
          <a href="#"><i class="fab fa-linkedin"></i></a>
        </div>
        <div class="footer-copyright">
        </div>
      </div>
    </div>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
